"use client";
import {getSession} from '@/lib/auth'; export type CapabilityState='DEMONSTRATED'|'DEVELOPING'|'INTRODUCED'|'NOT_EVALUATED'|'PROVISIONAL'; export type Evidence={id:string;capability:string;state:CapabilityState;verification:string;observedAt:string;context:string;artifact:string;probes:string[];freshness:string}; export type Result={sessionId:string;capability:string;completedAt:string;state:CapabilityState;summary:string;scorelessOutcome:string;evidenceId:string;telemetrySummary:string}; const key=(base:string)=>`${base}.${getSession()?.id||'anonymous'}`;
export const seedEvidence:Evidence[]=[{id:'self-seed-sql',capability:'SQL Window Functions',state:'DEMONSTRATED',verification:'CLIENT_EVALUATED',observedAt:'2026-09-17',context:'Transfer + boundary probe: ranking, chronological dedupe, tie/NULL resilience.',artifact:'sql-window-probe.sql',probes:['foundational formulation','structural transfer','boundary resilience'],freshness:'Current'},{id:'self-seed-python',capability:'Python Concurrency',state:'DEVELOPING',verification:'CLIENT_EVALUATED',observedAt:'2026-09-15',context:'Concurrency reasoning probe with rate-limiter and race-condition checks.',artifact:'rate-limiter.py',probes:['foundational formulation','partial transfer'],freshness:'Current'}];
export function loadEvidence(){if(typeof window==='undefined')return seedEvidence;try{const e=JSON.parse(localStorage.getItem(key('patima.evidence'))||'null');return Array.isArray(e)?e:seedEvidence}catch{return seedEvidence}} export function saveEvidence(e:Evidence[]){localStorage.setItem(key('patima.evidence'),JSON.stringify(e))} export function loadResults(){if(typeof window==='undefined')return [];try{const r=JSON.parse(localStorage.getItem(key('patima.results'))||'[]');return Array.isArray(r)?r:[]}catch{return[]}} export function saveResult(r:Result){const results=loadResults();localStorage.setItem(key('patima.results'),JSON.stringify([...results.filter(x=>x.sessionId!==r.sessionId),r]));const e=loadEvidence();const i=e.findIndex(x=>x.id===r.evidenceId);if(i>=0)e[i]={...e[i],state:r.state,observedAt:r.completedAt};else e.unshift({id:r.evidenceId,capability:r.capability,state:r.state,verification:'CLIENT_EVALUATED',observedAt:r.completedAt,context:r.summary,artifact:'assessment-output.txt',probes:['workspace probe'],freshness:'Current'});saveEvidence(e)}
export const assessments=[{slug:'sql-window-functions',title:'SQL Window Functions',duration:'12–18 min',description:'Work through ranking, sessionization, ties, and NULL behavior.',capability:'SQL Window Functions'},{slug:'python-concurrency',title:'Python Concurrency',duration:'12–18 min',description:'Reason about atomicity, token buckets, locks, and edge conditions.',capability:'Python Concurrency'}]; export const communityThreads=[{title:'Sessionization when timestamps collide',domain:'SQL',replies:8,verified:3},{title:'Async rate limiter fairness under burst load',domain:'Python',replies:5,verified:2},{title:'Idempotent streaming recovery pattern',domain:'Data Engineering',replies:11,verified:4}];
