"use client"; import Link from 'next/link'; import {useState} from 'react'; import {AppShell} from '@/components/AppShell'; const nodes=['SQL Window Functions','Python Concurrency','AWS Data Engineering','Distributed Consensus']; export default function HiringSearch(){const[s,setS]=useState(nodes.slice(0,2));return <AppShell role="employer"><p className="eyebrow">HIRING WORKSPACE</p><h1 className="mt-2 text-3xl font-semibold">Search verified capabilities</h1><p className="mt-2 text-sm text-slate-500">Define exact technical requirements. Results are factual and unranked.</p><div className="panel mt-6 p-5"><div className="label">Required capabilities · AND</div><div className="mt-3 flex flex-wrap gap-2">{nodes.map(n=><button key={n} onClick={()=>setS(v=>v.includes(n)?v.filter(x=>x!==n):[...v,n])} className={`rounded-lg border px-3 py-2 text-sm ${s.includes(n)?'border-emerald-400/30 bg-emerald-400/10 text-emerald-300':'border-white/5 text-slate-500'}`}>{n}</button>)}</div><Link href="/hiring/candidates" className="btn-primary mt-5">Search verified candidates →</Link></div></AppShell>}
