"use client"; import Link from 'next/link'; import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation'; import {signUp} from '@/lib/auth'; export default function Signup(){const r=useRouter(),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const submit=(e:FormEvent)=>{e.preventDefault();if(password.length<4){setError('Use at least 4 characters for this alpha build.');return}try{signUp(name,email);r.replace('/app')}catch(x){setError(x instanceof Error?x.message:'Could not create account')}};return <main className="grid min-h-screen place-items-center bg-[#071019] px-6 text-slate-100"><div className="w-full max-w-md"><Link href="/" className="text-sm text-slate-500">← PATIMA</Link><div className="panel mt-5 p-6 sm:p-8"><h1 className="text-xl font-semibold">Create your PATIMA account</h1><p className="mt-2 text-sm text-slate-500">Your candidate workspace is separate from employer views.</p><form onSubmit={submit} className="mt-6 space-y-4"><label className="field">Name<input className="input" value={name} onChange={e=>setName(e.target.value)} required/></label><label className="field">Email<input className="input" value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label className="field">Password<input className="input" value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></label>{error&&<div className="alert">{error}</div>}<button className="btn-primary w-full justify-center">Create candidate account</button></form><p className="mt-5 text-center text-sm text-slate-500">Already have an account? <Link className="text-emerald-300" href="/login">Sign in</Link></p></div></div></main>}
