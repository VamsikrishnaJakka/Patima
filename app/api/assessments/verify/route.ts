import{NextResponse}from'next/server';import{requireCandidate}from'@/lib/server-auth';import{withSessionClient}from'@/lib/db';import{dispatchAuthenticVerification}from'@/lib/verification/dispatcher';import{handleRouteError}from'@/lib/api-errors';
export async function POST(request:Request){try{const s=await requireCandidate(),b=await request.json(),sessionId=String(b.sessionId||''),variantId=String(b.variantId||''),code=String(b.candidateCode||'').slice(0,20000);if(!sessionId||!variantId||!code.trim())return NextResponse.json({error:'MISSING_REQUIRED_FIELDS'},{status:400});return await withSessionClient(s.userId,async(c)=>{const r=await c.query(`SELECT v.*,f.domain FROM question_variants v JOIN question_families f ON f.id=v.family_id JOIN active_question_reservations ar ON ar.variant_id=v.id JOIN assessment_sessions a ON a.id=ar.session_id WHERE v.id=$1 AND ar.session_id=$2 AND a.user_id=$3 AND a.status='IN_PROGRESS' AND ar.expires_at>clock_timestamp() FOR UPDATE OF ar`,[variantId,sessionId,s.userId]);if(!r.rows.length)return NextResponse.json({error:'VARIANT_NOT_FOUND_OR_LEASE_EXPIRED'},{status:404});return NextResponse.json(await dispatchAuthenticVerification({domain:r.rows[0].domain,candidateCode:code,variant:r.rows[0]}))})}catch(e){return handleRouteError(e,'assessments/verify')}}