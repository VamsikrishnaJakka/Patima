import {NextResponse} from 'next/server'; export async function GET(_:Request,{params}:{params:{id:string;evidence_id:string}}){return NextResponse.json({candidate_id:params.id,evidence_id:params.evidence_id,artifact:{code_diff:'SELECT user_id, event_time, SUM(is_new_session) OVER (PARTITION BY user_id ORDER BY event_time, event_id) AS session_id FROM session_deltas;',test_trace:[{name:'timestamp_collision_ties',status:'PASS',duration_ms:3.4}],verification:'PEER_ATTESTED'}})}
