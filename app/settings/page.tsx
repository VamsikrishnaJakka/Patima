import {AppShell} from '@/components/AppShell'; export default function Settings(){return <AppShell><p className="eyebrow">SETTINGS</p><h1 className="mt-2 text-3xl font-semibold">Privacy and account controls</h1><div className="mt-6 space-y-3">{[['Evidence visibility','Default: private to you. Sharing is explicit and scoped.'],['Assessment telemetry boundary','Only interactions inside the assessment workspace are eligible for provenance analysis.'],['Employer access','Use the candidate consent gateway for contact requests.']].map(([a,b])=><div className="panel p-5" key={a}><div className="font-medium">{a}</div><p className="mt-1 text-sm text-slate-500">{b}</p></div>)}</div></AppShell>}
