'use client';
import {AppShell} from '@/components/AppShell'; import {communityThreads} from '@/lib/personal'; export default function Communities(){return <AppShell><p className="eyebrow">TECHNICAL COMMUNITIES</p><h1 className="mt-2 text-3xl font-semibold">Problem-solving, not popularity</h1><p className="mt-2 text-sm text-slate-500">Structured technical discussions and verified solution versions.</p><div className="mt-6 space-y-3">{communityThreads.map(t=><article key={t.title} className="panel p-5 flex justify-between gap-4"><div><div className="text-xs text-emerald-300">{t.domain}</div><h2 className="mt-1 font-medium">{t.title}</h2></div><div className="text-xs text-slate-600">{t.replies} replies · {t.verified} verified</div></article>)}</div></AppShell>}
