"use client"; import {useEffect,useState} from 'react'; import {AppShell} from '@/components/AppShell'; import {getSession} from '@/lib/auth'; export default function Profile(){const[s,setS]=useState<any>(null);useEffect(()=>setS(getSession()),[]);return <AppShell><p className="eyebrow">PROFILE</p><h1 className="mt-2 text-3xl font-semibold">Your professional identity</h1><div className="panel mt-6 p-6 grid gap-5 sm:grid-cols-2"><div><div className="label">Name</div><p className="mt-1">{s?.name||'—'}</p></div><div><div className="label">Handle</div><p className="mt-1 font-mono">@{s?.handle||'—'}</p></div><div><div className="label">Account</div><p className="mt-1 text-slate-400">Candidate</p></div><div><div className="label">Privacy</div><p className="mt-1 text-slate-400">Employer contact requires consent.</p></div></div></AppShell>}
