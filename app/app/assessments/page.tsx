import Link from 'next/link'; import {AppShell} from '@/components/AppShell'; import {assessments} from '@/lib/personal'; export default function Assessments(){return <AppShell><p className="eyebrow">ASSESSMENTS</p><h1 className="mt-2 text-3xl font-semibold">Choose a technical probe</h1><p className="mt-2 text-sm text-slate-500">Your work stays inside the assessment workspace.</p><div className="mt-6 grid gap-4 md:grid-cols-2">{assessments.map(a=><Link href={`/app/assessments/${a.slug}`} key={a.slug} className="panel block p-5"><div className="flex justify-between text-xs text-slate-500"><span className="text-emerald-300">{a.capability}</span><span>{a.duration}</span></div><h2 className="mt-4 text-xl font-medium">{a.title}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{a.description}</p><div className="mt-5 text-xs text-emerald-300">Start assessment →</div></Link>)}</div></AppShell>}
