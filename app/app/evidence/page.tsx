"use client"; import {useEffect,useState} from 'react'; import {AppShell} from '@/components/AppShell'; import {loadEvidence,Evidence} from '@/lib/personal'; import {StatusPill} from '@/components/StatusPill'; export default function EvidencePage(){const[e,setE]=useState<Evidence[]>([]);useEffect(()=>setE(loadEvidence()),[]);return <AppShell><p className="eyebrow">EVIDENCE DOSSIER</p><h1 className="mt-2 text-3xl font-semibold">Your inspectable evidence</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">A dossier is a collection of observations, not a leaderboard.</p><div className="mt-6 space-y-4">{e.map(x=><article key={x.id} className="panel p-5"><div className="flex justify-between"><div><h2 className="font-medium">{x.capability}</h2><p className="mt-1 text-xs text-slate-600">Observed {x.observedAt} · {x.verification} · {x.freshness}</p></div><StatusPill value={x.state}/></div><div className="mt-5 grid gap-4 md:grid-cols-3"><div><div className="label">Context</div><p className="mt-1 text-sm leading-6 text-slate-400">{x.context}</p></div><div><div className="label">Probes</div><div className="mt-1 text-sm text-slate-400">{x.probes.join(' · ')}</div></div><div><div className="label">Artifact</div><p className="mt-1 font-mono text-sm text-slate-400">{x.artifact}</p></div></div></article>)}</div></AppShell>}
