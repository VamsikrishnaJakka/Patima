"use client"; import Link from 'next/link'; import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation'; import {LockKeyhole} from 'lucide-react'; import {signIn} from '@/lib/auth'; export default function Login(){const r=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const submit=(e:FormEvent)=>{e.preventDefault();try{const s=signIn(email,password);r.replace(s.role==='employer'?'/hiring/search':'/app')}catch(x){setError(x instanceof Error?x.message:'Sign in failed')}};return <main className="grid min-h-screen place-items-center bg-[#071019] px-6 text-slate-100"><div className="w-full max-w-md"><Link href="/" className="text-sm text-slate-500">← PATIMA</Link><div className="panel mt-5 p-6 sm:p-8"><div className="mb-6 flex items-center gap-3"><LockKeyhole className="h-5 w-5 text-emerald-300"/><div><h1 className="text-xl font-semibold">Sign in</h1><p className="text-sm text-slate-500">Return to your evidence workspace.</p></div></div><form onSubmit={submit} className="space-y-4"><label className="field">Email<input className="input" value={email} onChange={e=>setEmail(e.target.value)} type="email" required placeholder="you@example.com"/></label><label className="field">Password<input className="input" value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></label>{error&&<div className="alert">{error}</div>}<button className="btn-primary w-full justify-center">Sign in</button></form><div className="mt-5 rounded-lg border border-white/5 bg-white/[0.02] p-3 text-xs text-slate-500">Alpha candidate: <span className="font-mono text-slate-300">vamsi@example.com</span>. Employer demo: <span className="font-mono text-slate-300">recruiter@acme.example</span>.</div><p className="mt-5 text-center text-sm text-slate-500">New here? <Link className="text-emerald-300" href="/signup">Create account</Link></p></div></div></main>}
